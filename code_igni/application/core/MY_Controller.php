   

<?php
	
	// Base controller is CI_Controller, but in future if we want to edit base controller
	// because of the requirements if we have to edit the base controller then when we will update base framework
	// At that time we will loose all the data. So we are making a different base controller in core folder i.e. "My_Controller".
	class MY_Controller extends CI_Controller
	{
		
	}
?>